# Contributing

1. Create a branch from `main`.
2. Run `python manage.py test sketchmod` before opening a PR.
3. After codegen algorithm changes, run the model dump suite (see [development.md](docs/development.md)).
4. Keep commits focused by area (security, docs, features, tests).

Pull requests should describe behavior changes and include test updates when applicable.
